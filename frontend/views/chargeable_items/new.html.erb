<%= setup_context(:object => @chargeable_item) %>

<%= form_for @chargeable_item, :as => "chargeable_item", :url => {:action => :create}, :html => {:class => 'form-horizontal aspace-record-form', :id => "chargeable_item_form"} do |f| %>
  <%= form_context :chargeable_item, @chargeable_item do |form| %>
    <div class="row">
      <div class="col-md-3">
        <%# render_aspace_partial :partial => "sidebar" %>
      </div>

      <div class="col-md-9">
        <%= render_aspace_partial :partial => "shared/toolbar_new_records" %>
        <div class="record-pane">
          <h2><%= I18n.t("actions.new_prefix") %> <%= I18n.t("chargeable_item._singular") %> <span class="label label-info"><%= I18n.t("chargeable_item._singular") %></span></h2>
          <%= render_aspace_partial :partial => "chargeable_items/template", :locals => {:form => form} %>
        </div>
      </div>
    </div>
  <% end %>
<% end %>
